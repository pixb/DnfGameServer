# 批次17: TASK (续) 模块迁移计划

## 一、迁移范围

### 1.1 消息类型

| ModuleID | 消息类型 | 功能描述 | 状态 |
| :--- | :--- | :--- | :--- |
| 20004 | 任务进度更新 | 更新任务进度 | ✅ 待迁移 |
| 20005 | 任务放弃 | 放弃任务 | ✅ 待迁移 |
| 20006 | 任务奖励领取 | 领取任务奖励 | ✅ 待迁移 |
| 20007 | 任务追踪 | 追踪任务状态 | ✅ 待迁移 |

### 1.2 源文件

| 文件名 | 路径 | 状态 |
| :--- | :--- | :--- |
| REQ_TASK_PROGRESS.java | src/main/java/com/dnfm/mina/protobuf/REQ_TASK_PROGRESS.java | ✅ 待迁移 |
| REQ_TASK_ABANDON.java | src/main/java/com/dnfm/mina/protobuf/REQ_TASK_ABANDON.java | ✅ 待迁移 |
| REQ_TASK_REWARD.java | src/main/java/com/dnfm/mina/protobuf/REQ_TASK_REWARD.java | ✅ 待迁移 |
| REQ_TASK_TRACK.java | src/main/java/com/dnfm/mina/protobuf/REQ_TASK_TRACK.java | ✅ 待迁移 |

### 1.3 目标文件

| 文件名 | 路径 | 状态 |
| :--- | :--- | :--- |
| task_ext.proto | proto/dnf/v1/task_ext.proto | ✅ 待生成 |

## 二、迁移步骤

### 2.1 分析与准备

1. **分析源文件结构**：了解JProtobuf注解和字段定义
2. **确定类型映射**：JProtobuf类型到标准Protobuf类型的映射
3. **设计Proto文件**：基于源文件设计标准Protobuf消息结构

### 2.2 实现与生成

1. **创建task_ext.proto文件**：定义消息结构和数据类型
2. **配置代码生成**：设置buf.gen.yaml配置文件
3. **生成代码**：使用buf generate生成Java和Go代码

### 2.3 集成与测试

1. **扩展编解码器**：修改StandardProtobufDecoder和StandardProtobufEncoder
2. **编写测试用例**：验证消息序列化/反序列化
3. **编译验证**：确保Java代码编译通过

## 三、数据结构设计

### 3.1 核心消息结构

#### 3.1.1 任务进度更新

```protobuf
// 任务进度更新请求
message TaskProgressUpdateRequest {
  int32 character_id = 1;       // 角色ID
  int32 task_id = 2;             // 任务ID
  int32 objective_id = 3;        // 目标ID
  int32 progress = 4;            // 进度值
}

// 任务进度更新响应
message TaskProgressUpdateResponse {
  int32 error = 1;              // 错误码
  int32 character_id = 2;       // 角色ID
  int32 task_id = 3;             // 任务ID
  int32 objective_id = 4;        // 目标ID
  int32 current_progress = 5;     // 当前进度
  bool is_completed = 6;          // 是否完成
  string message = 7;            // 提示消息
}
```

#### 3.1.2 任务放弃

```protobuf
// 任务放弃请求
message TaskAbandonRequest {
  int32 character_id = 1;       // 角色ID
  int32 task_id = 2;             // 任务ID
}

// 任务放弃响应
message TaskAbandonResponse {
  int32 error = 1;              // 错误码
  int32 character_id = 2;       // 角色ID
  int32 task_id = 3;             // 任务ID
  bool success = 4;              // 是否成功
  string message = 5;            // 提示消息
}
```

#### 3.1.3 任务奖励领取

```protobuf
// 任务奖励领取请求
message TaskRewardClaimRequest {
  int32 character_id = 1;       // 角色ID
  int32 task_id = 2;             // 任务ID
}

// 任务奖励领取响应
message TaskRewardClaimResponse {
  int32 error = 1;              // 错误码
  int32 character_id = 2;       // 角色ID
  int32 task_id = 3;             // 任务ID
  bool success = 4;              // 是否成功
  string message = 5;            // 提示消息
  repeated TaskReward rewards = 6;  // 获得奖励
}
```

#### 3.1.4 任务追踪

```protobuf
// 任务追踪请求
message TaskTrackRequest {
  int32 character_id = 1;       // 角色ID
  int32 task_id = 2;             // 任务ID
  bool is_tracking = 3;          // 是否追踪
}

// 任务追踪响应
message TaskTrackResponse {
  int32 error = 1;              // 错误码
  int32 character_id = 2;       // 角色ID
  int32 task_id = 3;             // 任务ID
  bool is_tracking = 4;          // 是否追踪
  string message = 5;            // 提示消息
}
```

### 3.2 数据类型映射

| JProtobuf类型 | 标准Protobuf类型 | 备注 |
| :--- | :--- | :--- |
| int | int32 | 整数类型 |
| long | int64 | 长整数类型 |
| String | string | 字符串类型 |
| boolean | bool | 布尔类型 |
| List<T> | repeated T | 列表类型 |
| 自定义对象 | message | 嵌套消息 |

## 四、编解码器扩展

### 4.1 StandardProtobufDecoder

**新增方法：**

- `adaptTaskProgressUpdateRequest(byte[] body)` - 任务进度更新请求解码
- `adaptTaskAbandonRequest(byte[] body)` - 任务放弃请求解码
- `adaptTaskRewardClaimRequest(byte[] body)` - 任务奖励领取请求解码
- `adaptTaskTrackRequest(byte[] body)` - 任务追踪请求解码

**支持的ModuleID：**
- 20004 - 任务进度更新
- 20005 - 任务放弃
- 20006 - 任务奖励领取
- 20007 - 任务追踪

### 4.2 StandardProtobufEncoder

**新增方法：**

- `adaptTaskProgressUpdateResponse(Message msg)` - 任务进度更新响应编码
- `adaptTaskAbandonResponse(Message msg)` - 任务放弃响应编码
- `adaptTaskRewardClaimResponse(Message msg)` - 任务奖励领取响应编码
- `adaptTaskTrackResponse(Message msg)` - 任务追踪响应编码

**支持的ModuleID：**
- 20004 - 任务进度更新
- 20005 - 任务放弃
- 20006 - 任务奖励领取
- 20007 - 任务追踪

## 五、测试计划

### 5.1 单元测试

| 测试文件 | 测试内容 | 状态 |
| :--- | :--- | :--- |
| batch17_test.go | TaskProgressUpdateRequest/Response 序列化/反序列化 | ✅ 待实现 |
| batch17_test.go | TaskAbandonRequest/Response 序列化/反序列化 | ✅ 待实现 |
| batch17_test.go | TaskRewardClaimRequest/Response 序列化/反序列化 | ✅ 待实现 |
| batch17_test.go | TaskTrackRequest/Response 序列化/反序列化 | ✅ 待实现 |
| batch17_test.go | TaskReward 序列化/反序列化 | ✅ 待实现 |

### 5.2 编译验证

| 命令 | 验证内容 | 状态 |
| :--- | :--- | :--- |
| `mvn compile` | Java代码编译 | ✅ 待验证 |

## 六、时间计划

| 阶段 | 时间 | 任务 |
| :--- | :--- | :--- |
| 准备阶段 | 2026-02-09 | 分析源文件，设计Proto结构 |
| 实现阶段 | 2026-02-09 | 创建Proto文件，生成代码 |
| 集成阶段 | 2026-02-09 | 扩展编解码器，编写测试 |
| 验证阶段 | 2026-02-09 | 运行测试，验证编译 |
| 完成阶段 | 2026-02-09 | 更新文档，总结经验 |

## 七、风险评估

| 风险 | 概率 | 影响 | 应对措施 |
| :--- | :--- | :--- | :--- |
| 源文件不存在 | 中 | 高 | 基于功能需求设计消息结构 |
| 字段类型映射错误 | 低 | 中 | 参考已有批次的类型映射 |
| 编解码器集成失败 | 低 | 中 | 遵循已有批次的集成模式 |
| 测试用例失败 | 低 | 低 | 及时修复问题，确保功能正确 |

## 八、依赖关系

| 依赖项 | 版本 | 用途 | 状态 |
| :--- | :--- | :--- | :--- |
| protobuf | 3.21.0 | 协议定义 | ✅ 已就绪 |
| buf | 1.24.0 | 代码生成 | ✅ 已就绪 |
| go | 1.20+ | Go代码生成 | ✅ 已就绪 |
| java | 1.8+ | Java代码生成 | ✅ 已就绪 |

## 九、预期成果

1. **成功迁移任务扩展相关消息**：任务进度更新、任务放弃、任务奖励领取、任务追踪
2. **新增task_ext.proto文件**：定义任务扩展相关消息结构
3. **定义了任务扩展相关数据结构**：复用TaskReward等已有数据结构
4. **扩展了编解码器支持**：添加了任务扩展模块的编解码方法
5. **验证了跨语言通信**：确保Java服务器和Go客户端能够正确通信
6. **Go单元测试通过**：验证消息序列化/反序列化正确性
7. **Java编译成功**：确保代码集成无错误

## 十、后续计划

- **批次18**：迁移其他功能模块（如好友、邮件等）
- **持续优化**：根据实际使用情况优化消息结构和编解码器性能
