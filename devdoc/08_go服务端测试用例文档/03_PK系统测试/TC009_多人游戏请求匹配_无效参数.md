# TC009_多人游戏请求匹配_无效参数

## 测试概述

测试当使用无效参数请求多人游戏匹配时，匹配接口的返回结果。

## 前置条件

1. 已启动Go服务端（端口8082）
2. 已创建测试账号和角色
3. 已获取有效的authKey

## 测试步骤

1. 发送HTTP POST请求到 `/api/v1/pk/match` 接口，携带authKey和无效的匹配类型参数
2. 检查返回结果

## 预期结果

1. 接口返回失败
2. error字段不为0
3. 返回错误信息，提示参数无效

## 实际结果

## 结论

## 备注

