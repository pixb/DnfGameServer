# TC006_选择角色_无效角色GUID

## 测试概述

测试当玩家选择无效角色GUID时，角色选择接口的返回结果。

## 前置条件

1. 已启动Go服务端（端口8082）
2. 已创建测试账号和角色
3. 已获取有效的authKey

## 测试步骤

1. 发送HTTP POST请求到 `/api/v1/character/select` 接口，携带authKey和无效的charGuid参数
2. 检查返回结果

## 预期结果

1. 接口返回失败
2. error字段不为0
3. 返回错误信息，提示角色不存在

## 实际结果

## 结论

## 备注

