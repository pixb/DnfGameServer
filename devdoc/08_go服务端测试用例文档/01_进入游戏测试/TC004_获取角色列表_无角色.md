# TC004_获取角色列表_无角色

## 测试概述

测试当玩家账号下没有角色时，获取角色列表接口的返回结果。

## 前置条件

1. 已启动Go服务端（端口8082）
2. 已创建测试账号，但未创建角色
3. 已获取有效的authKey

## 测试步骤

1. 发送HTTP GET请求到 `/api/v1/character/list` 接口，携带authKey参数
2. 检查返回结果

## 预期结果

1. 接口返回成功
2. characters数组为空
3. error字段为0

## 实际结果

## 结论

## 备注

