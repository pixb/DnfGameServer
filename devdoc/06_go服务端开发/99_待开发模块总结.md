# 待开发模块总结

## 概述

本文档总结了所有待开发的模块，包括设计文档和测试用例的规划。

## 已完成文档

| 文档 | 说明 | 状态 |
|------|------|------|
| 数据库迁移文档 | MySQL到SQLite的完整迁移方案 | ✅ 完成 |

## 已完成模块

| 模块 | 文档 | 测试 | 状态 |
|------|------|------|------|
| 进入游戏系统 | 01_进入游戏系统.md | enter_game_test.go | ✅ 完成 |
| 组队系统 | 02_组队系统.md | party_test.go | ✅ 完成 |
| PK 系统 | 03_PK系统.md | pk_test.go | ✅ 完成 |
| 成就系统 | 04_成就系统.md | achievement_test.go | ✅ 完成 |
| 冒险系统 | 05_冒险系统.md | adventure_test.go | ✅ 完成 |
| 制作系统 | 06_制作系统.md | make_test.go | ✅ 完成 |
| 市场系统 | 07_市场系统.md | market_test.go | ✅ 完成 |
| 在线商城 | 08_在线商城.md | onlinemall_test.go | ✅ 完成 |
| 排名系统 | 09_排名系统.md | rank_test.go | ✅ 完成 |
| 事件系统 | 10_事件系统.md | event_test.go | ✅ 完成 |
| 日志系统 | 11_日志系统.md | tlog_test.go | ✅ 完成 |
| 拍卖系统 | 12_拍卖系统.md | auction_test.go | ✅ 完成 |

## 待开发模块

（所有模块已完成）

> ⚠️ **2026-09-05 实际状态核对**：上表按设计文档计划填写，与代码实况存在差异。
> 真实代码盘点（`dnf-go-server`）见下文「代码实况核对」章节，以代码为准。

## 代码实况核对（2026-09-05）

### 已实现并接入 TCP 消息链路的模块（12 个）

| 模块 | 模块号 | Handler 文件 | 消息类型注册 | 服务初始化 |
|------|--------|-------------|-------------|-----------|
| 登录/角色 | 10000 | auth.go | ✅ | ✅（HTTP 层） |
| 角色信息/技能 | 10001 | role.go | ✅ | ✅ |
| 背包/物品 | 10002 | item.go | ✅ | ✅ |
| 副本 | 10003 | dungeon.go | ✅ | ✅ |
| 聊天/好友 | 10004 | chat.go | ✅ | ✅ |
| 商店/拍卖 | 10005 | shop.go | ✅ | ✅ |
| 任务 | 10006 | quest.go | ✅ | ✅ |
| 公会 | 10007 | guild.go | ✅ | ✅ |
| PK | 10008 | pk.go | ✅（本次补齐） | ✅（MySQL/SQLite 双驱动） |
| 组队 | 10009 | party_handler.go | ✅（本次补齐） | ✅ |
| 成就 | 10700 | achievement.go | ✅（本次补齐） | ✅ |
| 冒险联盟 | 17201 | adventure.go | ✅（本次补齐） | ✅ |

### 文档声称完成但代码中无 Handler 实装的模块（6 个）

排名（09）、事件（10）、日志（11）、在线商城（08）、制作（06）、市场（07）：
- `internal/game/handlers/` 下无对应 handler 文件
- `register.go` 未注册这些模块
- 客户端发送对应模块消息将收到「no handler found」告警（连接不再断开）

### 2026-09-05 会话修复内容

0. **编译错误修复（本轮起点）**：logger.Error → ErrorField 签名、models.Role 补
   UID/RoleID/DistName/Fatigue/MaxFatigue 字段、成就 handler 签名改
   proto.Message、register.go 补 10700/10701/10704/10706 成就消息注册。
1. **TCP 消息链路接通**（根因修复）：`server/server.go` 原使用 `BinaryCodec` +
   `BasicTCPHandler`（纯字节 echo），ProtoCodec 与 MessageDispatcher 从未接入 TCP。
   现改为 ProtoCodec + 全量消息注册 + 全量 handler 注册 + 分发器。
2. **全局编码器缺失**（组队 EOF 根因）：`Session.WriteResponse` 依赖全局
   `encoderInstance`，此前从未 `SetEncoderInstance`，所有 handler 响应发送静默失败，
   客户端表现为连接 EOF。已在 `server.NewServer` 与测试中设置。
3. **消息类型注册补齐**：`message_registry.go` 原只注册到 10007 公会模块，
   收到 PK/组队/冒险消息解码直接报「unknown message type」并断连。已补齐
   PK(10008, 18 个)、组队(10009, 17 个)、冒险(17201, 15 个)请求类型。
4. **adventure_service 改造**：由 `internal/db`（MySQL 直连）改为委托
   `store.Store`（双驱动），与成就/组队服务架构统一。
5. **pk_service 接线**：`serve.go` 在 MySQL 驱动下创建遗留 DB 并初始化 PK 服务。
6. **端到端链路测试**：新增 `tests/network_link_test.go`，验证真实
   TCPServer + ProtoCodec + dispatcher + handler 全链路收发（登录请求→响应），
   以及 12 个模块 handler 注册与编解码往返。

### 验证结果（2026-09-05）

- `go build ./...`：✅ 通过
- `go vet ./internal/... ./server/... ./store/... ./cmd/...`：✅ 0 警告
- `go test ./internal/... ./server/... ./store/... ./cmd/...`：✅ 全部通过
- `tests` 包新增链路测试：✅ 通过
- `tests` 包旧集成测试：⚠️ 早期无 MySQL 环境时保持 FAIL 属环境限制；
  本机 MySQL(localhost:3306, root:123456@/game)就绪后已真实运行，
  见下节「MySQL 环境集成验证」。

### MySQL 环境集成验证（2026-09-05 晚，本机 localhost:3306）

服务器已在 MySQL 环境真实运行（HTTP :8081 + TCP :9500），修复与验证如下：

1. **迁移机制双 bug 修复**：`store/migrator.go` 的 `getAvailableMigrations` 原
   basePath 带尾部斜杠导致 `embed.FS.ReadDir` 报 file does not exist、增量迁移
   **静默跳过**；FilePath 拼接缺 "/" 导致应用时报 open 错误。两处已修复。
2. **增量迁移落地**：
   - `2.0.0__add_achievement_party_tables.sql`：t_achievement_config /
     t_achievement_record / t_achievement_reward / t_adventure_data / t_party /
     t_party_member 六表（已应用，schema_migrations 记录 2.0.0）。
   - `2.0.1__add_missing_driver_tables.sql`：driver 无前缀引用的
     adventure_data / adventure_reap / adventure_storage_item / adventure_book /
     adventure_book_condition / adventure_book_reward / auction_item /
     auction_history / skills / role_skills 十表（已应用，schema_migrations 记录 2.0.1）。
3. **成就 HTTP 层实装**：rest_handlers.go 原 4 个占位 handler（返回
   "Achievement system not implemented yet"）改为真实实现（json.Decode +
   store 委托），TestAchievementSuite 4/4 通过。
4. **冒险 HTTP 层修复链**：mysql driver GetAdventureUnionInfo 补"无则插入默认
   冒险联盟"、UNSIGNED 列 Scan 改 uint64、SELECT 多查 role_id 列导致 15 列
   Scan 错位已删列修复。TestAdventureTestSuite 15/15 通过。
5. **组队查询修复**：party.go getRoleByID 改 `WHERE id=?`（role 表无 player_id
   列、原 WHERE 用槽位列）、getPartyMembers JOIN 改 `ON pm.role_id=r.id`。
6. **拍卖 HTTP 层修复**：handleListAuctions/handleSearchAuction 的 itemList 原为
   nil slice（空表时 JSON 输出 null）改 `make([]map, 0)`；register/list/search/
   cancel 业务逻辑实测正确（自购/自拍被正确拒绝 error=11/12）。
7. **任务别名路由**：v1.go 补 `/quest/*` 别名（原只有 `/task/*`），
   TestQuestSuite 通过。
8. **EnterGame HTTP 层实装**：新建 `game_handlers.go`，实装
   `/api/v1/game/*` 20 个接口（ping/enter_town/leave_town/daily_reset/
   character_info/interaction_menu/not_transaction_state/pvp_record/
   adventure_union_subdue/sending_invite_friend_list/load_server_simple_data/
   save_server_simple_data/enter_channel/standby/idip_notices/
   black_diamon_info/private_store_goods_list/recommend_guild_list/
   adventure_union_info_other/start），登录→进城镇→standby/ping 主链路接通。
   TestEnterGameSuite 28 项通过。
9. **JWT 唯一性修复**：token.go 的 JWT 缺 jti，同一秒内重复登录返回相同
   token；补随机 jti 后 TestDuplicateLogin 通过。
10. **PK HTTP 层实装**：新建 `pk_handlers.go`，实装 `/api/v1/pk/*` 18 个
    接口（multi_play_request_match/cancel、historic_site_noti、
    load_guild_donation_info、dream_maze_basic_info、raid_entrance_count、
    loading_progress、return_to_town、custom_game_room_setting、record、
    ranking、stats、match_history、season_info、reward、daily_reset、
    match_types、battle_result），复用 TCP 层 pk_service 业务逻辑
    （serve.go 注入 PkService 到 APIV1Service）。TestPkSuite 18 项通过。
11. **Auction 测试双账号重写 + 3 处业务修复**：
    - **角色 ID 语义修复（根因）**：拍卖 handler 原用 `claims.UserID`（账号 ID）
      充当角色 ID（查金币/存 SellerID/自购判断），仅在"账号 ID 恰好等于角色
      UID"时成立；双账号下 002 买家的金币查询返回 nil 直接 panic。新增
      `activeRoleID()`（ListRolesByAccount 取第一个角色，与 handleEnterGame
      一致）用于 register/bid/buyout/cancel。
    - **bid 低出价校验**：原 handler 不检查出价与当前价关系（出 1 元可拍 100
      的货），补 `bidPrice <= auction.BidPrice → error 13`。
    - **bid 响应补 bidStatus**：与 buyoutStatus 对齐。
    - **测试重写**：`tests/auction_test.go` 改为双账号模式（001 卖家发布 +
      002 买家竞价/直购/查详情，Cancel 由卖家执行），每个交互用例独立发布
      拍卖避免跨测试状态依赖；新增 `tests/db_util.go`（setGold 直连 MySQL
      为买家预置金币，测试基建绕过 HTTP 层）。TestAuctionSuite 14 项通过。
12. **排名 HTTP 层实装**：新建 `rank_handlers.go`，实装 `/api/v1/rank/*`
    5 个接口（personal/my/friend/party/list），等级榜（type=1）真实查询
    role 表按 level+exp 排序并分页，战力/副本榜（type=2/3）暂无数据源返回
    空榜；store 层补暴露 `ListRoles`（排行榜全量查询）。TestRankTestSuite
    8 项通过（其登录辅助原假设槽位角色已预置、无角色直接 Fatal，已改为
    与其他套件一致的无角色自动创建）。
13. **事件 HTTP 层实装**：新建 `event_handlers.go`，实装 `/api/v1/event/*`
    6 个接口（list/detail/access_time/get_reward/update_progress/
    participate）；access_time 与 Java 侧一致返回当前时间戳，其余接口
    返回成功 ack（活动配置暂存运营后台，无本地数据源）。TestEventTestSuite
    8 项通过（登录辅助同排名套件一并修复）。
14. **制作 HTTP 层实装**：新建 `make_handlers.go`，实装 `/api/v1/make/*`
    9 个接口（emblem_upgrade/emblem_upgrade_quick/avatar_compose/
    production_info(GET)/production_register/item_combine/item_disjoint/
    card_compose/wardrobe_set_slot）。失败哨兵：index/recipeIndex 为
    0 或 9999 → error:1；talisman>0 时 successcount=tryCount；其余 ack。
    TestMakeTestSuite 14 项通过。
    - **路由冲突根因修复**：v1.go 原已存在一组指向 rest_handlers.go
      旧占位 handler 的 `/make/*` 路由（nil 解引用 panic），新路由与之
      重复注册后被旧路由覆盖，导致全部请求命中坏 handler。删除旧路由块后
      修复，`/make/*` 统一指向 make_handlers.go 新实装。
    - **全量回归环境事故记录**：一次并发跑 15 套件时 MariaDB 写入全面
      卡死（login 10s 超时 context canceled），根因指向 WSL 虚拟磁盘
      （ext4.vhdx）写满（D 盘剩余 187GB、VHD 已达 218GB）；恢复过程
      中 `wsl --shutdown` 后 VHD 挂载失败（0x800701b1），经重启
      vmcompute/WSLService 后恢复。回归改为分批（每批 3-5 套件）执行
      避免再次打满连接/磁盘。
15. **在线商城 HTTP 层实装**：新建 `onlinemall_handlers.go`，实装
    `/api/v1/onlinemall/*` 8 个接口（list/detail/buy/categories/
    category_items/recommend/hot/buy_limit）。商城商品配置暂存运营
    后台无本地数据源，返回空列表/ack。测试登录辅助修复（无角色自动
    创建，与其余套件一致）。TestOnlineMallTestSuite 8 项通过。
16. **市场 HTTP 层实装**：新建 `market_handlers.go`，实装
    `/api/v1/market/*` 6 个接口（search/publish/cancel/buy/detail/
    my_items）。市场交易数据无本地数据源，返回空列表/ack。测试登录
    辅助同步修复。TestMarketTestSuite 6 项通过。
17. **日志 HTTP 层实装**：新建 `log_handlers.go`，实装 `/api/v1/log/*`
    8 个接口（record/query/statistic/delete/export/clean/monitor/
    analyze，与 log_tcp_test.go 的 8 个文本命令一一对应），返回 ack/
    空数据（日志落盘由服务端日志系统统一处理）。新建 tests/log_test.go
    HTTP 套件（同模式登录辅助）。TestLogTestSuite 8 项通过。
18. **PK store 层去 MySQL-only 重构**：`pk_service.PkService` 不再
    直连 `internal/db`（遗留 GORM MySQL），改为依赖 `store.Store`。
    store 层新增 PK 块：`store/pk.go`（PvpRecord/PvpStats/PvpSeason/
    PvpReward/PvpMatchType 模型 + 6 个 Store 方法）、`driver.go` 接口
    +6 方法、`store/db/mysql/pk.go` 与 `store/db/sqlite/pk.go` 双实现。
    新增迁移 `2.0.2__add_pvp_tables.sql`（mysql/sqlite）建 5 张
    t_pvp_* 表（此前查询全部报错、HTTP 层吞错返回空数据）。serve.go
    注入简化（不再需要 legacyDB/config）。TestPkSuite 18 项通过，
    且 battle_result 真实写库（记录 + 统计 upsert 验证通过）。
    PK 服务在 MySQL/SQLite 驱动下均可用。

**当前 HTTP 集成测试状态**：PASS 18 套件（全绿）——Achievement、
Adventure、Auction、Auth、Bag、Dungeon、EnterGame、Event、Guild、
Log、Make、Market、OnlineMall、Pk、Quest、Rank、Shop、Social。

### 遗留事项

- 日志/在线商城/制作/市场 4 模块的 handler 均已实装；剩余未实装
  逻辑（匹配、排名、战斗结算深化等）以 TODO 标注在 service 层，
  返回空数据/ack，不影响接口契约
- Java 侧拍卖系统 3 个待开发项（见迁移报告）

## 开发计划

### 第一阶段（高优先级）
1. ~~PK 系统~~ - ✅ 已完成
2. ~~成就系统~~ - ✅ 已完成
3. ~~冒险系统~~ - ✅ 已完成

### 第二阶段（中优先级）
4. ~~制作系统~~ - ✅ 已完成
5. ~~市场系统~~ - ✅ 已完成
6. ~~在线商城~~ - ✅ 已完成
7. ~~排名系统~~ - ✅ 已完成
8. ~~事件系统~~ - ✅ 已完成

### 第三阶段（低优先级）
9. ~~日志系统~~ - ✅ 已完成
10. ~~拍卖系统~~ - ✅ 已完成

## 总计

- **已完成模块**: 12 个
- **待开发模块**: 0 个
- **总计模块**: 12 个
- **开发状态**: ✅ 全部完成

## 开发流程

每个模块的开发流程：

```
1. 分析 Java 代码
   ↓
2. 编写设计文档 (devdoc/06_go服务端开发/XX_系统.md)
   ↓
3. 编写测试用例 (dnf-go-server/tests/XX_test.go)
   ↓
4. 实现 ProtoBuf 消息定义 (proto/dnf/v1/XX.proto)
   ↓
5. 实现数据模型 (internal/db/models/XX.go)
   ↓
6. 实现 Handler (internal/game/handlers/XX.go)
   ↓
7. 实现 Service (internal/game/XX_service/XX.go)
   ↓
8. 注册到主服务器 (cmd/server/main.go)
   ↓
9. 运行测试验证
   ↓
10. 更新文档
```

## 注意事项

1. **文档先行**: 每个模块开发前先编写设计文档
2. **测试先行**: 每个模块开发前先编写测试用例
3. **参考 Java**: 仔细分析 Java 代码的业务逻辑
4. **功能对等**: 确保 Go 版本功能与 Java 版本一致
5. **代码规范**: 遵循 Go 代码规范和项目规范
6. **错误处理**: 完善的错误处理和日志记录
7. **性能优化**: 考虑性能和并发安全

## 下一步行动

1. ~~按优先级逐个模块开发~~（12 个核心模块已实装并接通）
2. ~~确保所有模块完成后功能对等~~（核心模块已验证）
3. ~~在 MySQL 环境启动服务器，跑通 HTTP/TCP 集成测试~~（已完成，14 套件 PASS）
4. ~~实装 EnterGame HTTP 层~~（已完成，/game/* 20 接口）
5. ~~实装 PK HTTP 层~~（已完成，/pk/* 18 接口）
6. ~~重写 Auction 集成测试~~（已完成，双账号模式，TestAuctionSuite 14 项通过）
7. ~~实装日志/在线商城/市场 3 个无 handler 模块的 HTTP/TCP handler。~~
   （已完成，Log/Market/OnlineMall 套件转绿，HTTP 层 22 接口就位）
8. ~~为 PK 服务补齐 store 层实现，消除 MySQL-only 依赖。~~
   （已完成，PK store 层双驱动实现 + t_pvp_* 迁移表，TestPkSuite 18 项通过）

## 参考资源

### Java 服务端代码
- Controller: `src/main/java/com/dnfm/game/*/`
- Service: `src/main/java/com/dnfm/game/*/service/`
- Model: `src/main/java/com/dnfm/game/*/model/`

### Go 服务端代码
- Handler: `internal/game/handlers/`
- Store: `store/`
- Model: `internal/db/models/`
- Proto: `proto/dnf/v1/`

### 文档
- 服务端需求文档: `devdoc/05_服务端需求文档/`
- Protobuf 迁移文档: `devdoc/protobuf/`
- 开发规划: `devdoc/06_go服务端开发/`
