server:
  tcp:
    host: "0.0.0.0"
    port: 9000
    backlog: 3000
  http:
    host: "0.0.0.0"
    port: 8080

database:
  driver: "sqlite"
  dsn: "./game.db"

cache:
  default_expiration: 300
  cleanup_interval: 600

log:
  level: "info"
  is_development: true
