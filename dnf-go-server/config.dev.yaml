# DNF Go Server Development Configuration
# 使用SQLite进行开发测试

# 服务器配置
port: 8080

# 数据库配置
driver: sqlite
dsn: ./data/dnf_dev.db
mode: dev

# 日志配置
log:
  level: debug
  format: console

# 游戏配置
game:
  max_players: 1000
  max_roles_per_account: 4
  default_map_id: 1
  default_channel: 1
