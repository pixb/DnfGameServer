# 05_跨语言通信测试

## 📋 概述

本文档详细说明跨语言通信测试的方法和步骤，验证 Go 客户端与 Java 服务端之间的通信是否正常。

## 🎯 测试目标

验证以下功能：
1. ✅ Go 客户端能发送请求
2. ✅ Java 服务端能接收请求
3. ✅ Java 服务端能发送响应
4. ✅ Go 客户端能接收响应
5. ✅ 跨语言通信正常
6. ✅ 所有字段正确解析

## 🌐 测试环境准备

### 1. Java 服务端

**要求**：
- Java 服务端已启动
- 端口 10001 可访问
- 配置使用标准 Protobuf 模式

**启动命令**：
```bash
cd /home/pix/dev/code/java/DnfGameServer
./mvnw spring-boot:run
```

**验证**：
```bash
# 检查端口是否监听
lsof -i :10001

# 或使用 netstat
netstat -an | grep 10001
```

**配置**：
```properties
# application.properties
protobuf.mode=standard
server.port=10001
```

### 2. Go 客户端

**要求**：
- Go 客户端代码已编写
- 使用生成的 Protobuf 代码
- 能连接到 Java 服务端

**目录结构**：
```
dnf-go-client/
├── gen/
│   └── go/
│       └── dnf/
│           └── v1/
│               └── auth_login.pb.go
└── test/
    └── main.go
```

**依赖**：
```bash
cd /home/pix/dev/code/java/DnfGameServer/dnf-go-client
go mod init dnf-go-client
go get google.golang.org/protobuf/proto
```

## 📨 消息头结构

### 协议格式

```
+--------+--------+--------+--------+--------+----------------+
| 总长度 | 模块ID | 序列号 | 事务ID | 保留  | 消息体 |
| 2字节  | 2字节  | 1字节  | 1字节  | 2字节  | N字节   |
+--------+--------+--------+--------+--------+----------------+
```

### 字段说明

| 字段 | 大小 | 类型 | 说明 |
| :--- | :--- | :--- | :--- |
| 总长度 | 2 字节 | uint16 | 消息体长度 + 8 字节 |
| 模块ID | 2 字节 | uint16 | 消息模块标识符 |
| 序列号 | 1 字节 | uint8 | 消息序列号（固定为 1） |
| 事务ID | 1 字节 | uint8 | 事务标识符 |
| 保留 | 2 字节 | uint16 | 保留字段（固定为 0） |
| 消息体 | N 字节 | bytes | Protobuf 序列化后的消息 |

### 字节序

- **LittleEndian**：小端字节序

## 💻 Go 客户端实现

### 消息头构建

```go
package main

import (
    "bytes"
    "encoding/binary"
)

func buildMessageHeader(moduleID uint16, seq uint8, transactionID uint8, body []byte) []byte {
    totalLen := uint16(len(body) + 8)
    
    buf := new(bytes.Buffer)
    binary.Write(&buf, binary.LittleEndian, totalLen)
    binary.Write(&buf, binary.LittleEndian, moduleID)
    buf.WriteByte(seq)
    buf.WriteByte(transactionID)
    binary.Write(&buf, binary.LittleEndian, uint16(0))
    buf.Write(body)
    
    return buf.Bytes()
}
```

### 消息头解析

```go
package main

import (
    "bytes"
    "encoding/binary"
)

type MessageHeader struct {
    TotalLen      uint16
    ModuleID      uint16
    Seq           uint8
    TransactionID uint8
    Reserved      uint16
}

func parseMessageHeader(data []byte) MessageHeader {
    buf := bytes.NewReader(data)
    
    var header MessageHeader
    binary.Read(&buf, binary.LittleEndian, &header.TotalLen)
    binary.Read(&buf, binary.LittleEndian, &header.ModuleID)
    binary.Read(&buf, binary.LittleEndian, &header.Seq)
    binary.Read(&buf, binary.LittleEndian, &header.TransactionID)
    binary.Read(&buf, binary.LittleEndian, &header.Reserved)
    
    return header
}
```

### 完整的 Go 客户端示例

```go
package main

import (
    "bytes"
    "encoding/binary"
    "fmt"
    "log"
    "net"
    "time"
    
    dnfv1 "dnf-go-client/gen/go/dnf/v1"
    "google.golang.org/protobuf/proto"
)

func main() {
    log.Println("=== 开始测试标准Protobuf跨语言通信 ===")
    
    conn, err := net.Dial("tcp", "127.0.0.1:10001")
    if err != nil {
        log.Fatalf("连接服务器失败: %v", err)
    }
    defer conn.Close()
    log.Println("连接服务器成功: 127.0.0.1:10001")
    
    req := &dnfv1.LoginRequest{
        Openid:         "hutue",
        Type:           1,
        Token:          "test_token_abc",
        PlatID:         111,
        ClientIP:       "127.0.0.1",
        Version:        "1.0.0",
        Friendopenid:   "",
        Cancelunregist: 0,
        Countrycode:    "CN",
        Toyplatid:      1,
        Agetype:        1,
    }
    
    log.Printf("发送登录请求: openid=%s, token=%s, platID=%d\n", 
        req.Openid, req.Token, req.PlatID)
    
    reqData, err := proto.Marshal(req)
    if err != nil {
        log.Fatalf("请求序列化失败: %v", err)
    }
    log.Printf("请求序列化成功，长度=%d\n", len(reqData))
    
    reqMsg := buildMessageHeader(10000, 1, 0, reqData)
    log.Printf("消息头构建成功，总长度=%d\n", len(reqMsg))
    
    _, err = conn.Write(reqMsg)
    if err != nil {
        log.Fatalf("发送请求失败: %v", err)
    }
    log.Println("发送请求成功")
    
    conn.SetReadDeadline(time.Now().Add(10 * time.Second))
    
    headerBuf := make([]byte, 8)
    _, err = conn.Read(headerBuf)
    if err != nil {
        log.Fatalf("接收响应头失败: %v", err)
    }
    
    header := parseMessageHeader(headerBuf)
    log.Printf("响应消息头: totalLen=%d, moduleID=%d, seq=%d, transactionID=%d\n",
        header.TotalLen, header.ModuleID, header.Seq, header.TransactionID)
    
    bodyLen := int(header.TotalLen) - 8
    bodyBuf := make([]byte, bodyLen)
    _, err = conn.Read(bodyBuf)
    if err != nil {
        log.Fatalf("接收响应体失败: %v", err)
    }
    log.Printf("响应消息体长度=%d\n", len(bodyBuf))
    
    var resp dnfv1.LoginResponse
    err = proto.Unmarshal(bodyBuf, &resp)
    if err != nil {
        log.Fatalf("响应反序列化失败: %v", err)
    }
    
    log.Println("=== 登录响应解析成功 ===")
    log.Printf("Error: %d\n", resp.Error)
    log.Printf("Authkey: %s\n", resp.Authkey)
    log.Printf("Accountkey: %s\n", resp.Accountkey)
    log.Printf("Encrypt: %t\n", resp.Encrypt)
    log.Printf("Servertime: %d\n", resp.Servertime)
    log.Printf("Localtime: %s\n", resp.Localtime)
    log.Printf("Authority: %d\n", resp.Authority)
    log.Printf("Key: %s\n", resp.Key)
    log.Printf("Worldid: %d\n", resp.Worldid)
    
    log.Println("频道列表:")
    for i, channel := range resp.Channel {
        log.Printf("  [%d] World=%d, Channel=%d, IP=%s, Port=%d, Priority=%d\n",
            i, channel.World, channel.Channel, channel.Ip, channel.Port, channel.Priority)
    }
    
    log.Println("=== 测试成功 ===")
}
```

## 🔍 测试验证要点

### 1. 消息头解析正确

**验证内容**：
- 总长度：消息体长度 + 8 字节
- 模块ID：正确的模块标识符
- 序列号和事务ID正确

**验证方法**：
```go
header := parseMessageHeader(headerBuf)
log.Printf("响应消息头: totalLen=%d, moduleID=%d, seq=%d, transactionID=%d\n",
    header.TotalLen, header.ModuleID, header.Seq, header.TransactionID)
```

**预期结果**：
- `totalLen` = 消息体长度 + 8
- `moduleID` = 10000（登录响应）
- `seq` = 1
- `transactionID` = 0

### 2. 请求消息解析正确

**验证内容**：
- 所有字段正确解析
- 类型转换正确
- 边界值处理正确

**验证方法**：
```go
req := &dnfv1.LoginRequest{
    Openid:         "hutue",
    Type:           1,
    Token:          "test_token_abc",
    PlatID:         111,
    ClientIP:       "127.0.0.1",
    Version:        "1.0.0",
    Friendopenid:   "",
    Cancelunregist: 0,
    Countrycode:    "CN",
    Toyplatid:      1,
    Agetype:        1,
}

reqData, err := proto.Marshal(req)
if err != nil {
    log.Fatalf("请求序列化失败: %v", err)
}
```

**预期结果**：
- 序列化成功
- 序列化后的字节数组长度正确

### 3. 响应消息解析正确

**验证内容**：
- 所有字段正确解析
- 嵌套对象正确处理
- 列表类型正确处理

**验证方法**：
```go
var resp dnfv1.LoginResponse
err = proto.Unmarshal(bodyBuf, &resp)
if err != nil {
    log.Fatalf("响应反序列化失败: %v", err)
}

log.Println("=== 登录响应解析成功 ===")
log.Printf("Error: %d\n", resp.Error)
log.Printf("Authkey: %s\n", resp.Authkey)
log.Printf("Accountkey: %s\n", resp.Accountkey)
log.Printf("Encrypt: %t\n", resp.Encrypt)
log.Printf("Servertime: %d\n", resp.Servertime)
log.Printf("Localtime: %s\n", resp.Localtime)
log.Printf("Authority: %d\n", resp.Authority)
log.Printf("Key: %s\n", resp.Key)
log.Printf("Worldid: %d\n", resp.Worldid)

log.Println("频道列表:")
for i, channel := range resp.Channel {
    log.Printf("  [%d] World=%d, Channel=%d, IP=%s, Port=%d, Priority=%d\n",
        i, channel.World, channel.Channel, channel.Ip, channel.Port, channel.Priority)
}
```

**预期结果**：
- 反序列化成功
- 所有字段值正确
- 嵌套对象和列表正确处理

### 4. 编解码器工作正常

**验证内容**：
- StandardProtobufDecoder 成功解析标准 Protobuf 消息
- StandardProtobufEncoder 成功编码标准 Protobuf 消息
- 消息适配层正确处理转换

**验证方法**：
- 查看服务端日志，确认编解码器被正确调用
- 检查消息适配方法的调用情况

**预期结果**：
- 服务端日志显示编解码器被正确调用
- 消息适配方法正确处理转换

## 📊 测试成功标准

### Go 侧验证

- ✅ Go 客户端能连接到 Java 服务端
- ✅ Go 客户端能发送请求
- ✅ Go 客户端能接收响应
- ✅ 请求消息序列化成功
- ✅ 响应消息反序列化成功
- ✅ 所有字段正确解析

### Java 侧验证

- ✅ Java 服务端能接收请求
- ✅ Java 服务端能发送响应
- ✅ StandardProtobufDecoder 被正确调用
- ✅ StandardProtobufEncoder 被正确调用
- ✅ 消息适配方法正确处理转换

### 集成验证

- ✅ Go 客户端能发送请求
- ✅ Java 服务端能接收请求
- ✅ Java 服务端能发送响应
- ✅ Go 客户端能接收响应
- ✅ 跨语言通信正常
- ✅ 所有字段正确解析

## 🐛 常见问题

### 问题 1: 连接超时

**现象**：Go 客户端连接 Java 服务端超时

**原因**：Java 服务端未启动或端口不可访问

**解决方案**：
1. 检查 Java 服务端是否启动
2. 检查端口 10001 是否可访问
3. 检查防火墙设置

### 问题 2: 消息头解析错误

**现象**：解析消息头时出现错误

**原因**：消息头格式不正确或字节序错误

**解决方案**：
1. 确认消息头格式正确
2. 确认使用 LittleEndian 字节序
3. 检查消息头长度是否为 8 字节

### 问题 3: 消息体解析错误

**现象**：解析消息体时出现错误

**原因**：消息体长度不正确或 Protobuf 格式错误

**解决方案**：
1. 确认消息体长度正确
2. 使用 `protoc --decode_raw` 检查消息体
3. 确认 Proto 文件定义正确

### 问题 4: 字段值不匹配

**现象**：解析后的字段值与预期不符

**原因**：字段序号不匹配或类型映射错误

**解决方案**：
1. 检查字段序号是否一致
2. 检查类型映射是否正确
3. 参考 [02_类型映射.md](./02_类型映射.md) 中的类型映射表

## 📝 测试报告模板

### 测试基本信息

| 项目 | 内容 |
| :--- | :--- |
| 测试日期 | 2026-02-09 |
| 测试人员 | [姓名] |
| 测试环境 | WSL Ubuntu 20.04 |
| Java 版本 | 1.8.0_xxx |
| Go 版本 | 1.21.x |

### 测试结果

| 测试项 | 结果 | 备注 |
| :--- | :--- | :--- |
| Go 客户端连接 Java 服务端 | ✅ 通过 | - |
| Go 客户端发送请求 | ✅ 通过 | - |
| Java 服务端接收请求 | ✅ 通过 | - |
| Java 服务端发送响应 | ✅ 通过 | - |
| Go 客户端接收响应 | ✅ 通过 | - |
| 请求消息序列化 | ✅ 通过 | 长度=53 |
| 响应消息反序列化 | ✅ 通过 | 长度=117 |
| 所有字段正确解析 | ✅ 通过 | - |

### 测试输出

**Go 客户端输出**：
```
=== 开始测试标准Protobuf跨语言通信 ===
连接服务器成功: 127.0.0.1:10001
发送登录请求: openid=hutue, token=test_token_abc, platID=111
请求序列化成功，长度=53
消息头构建成功，总长度=61
发送请求成功
接收响应成功，长度=117
响应消息头: totalLen=117, moduleID=10000, seq=1, transactionID=0
响应消息体长度=109
=== 登录响应解析成功 ===
Error: 0
Authkey: 78575fa2-2c0d-46a6-9c6c-c241c5769a6d
Accountkey: 397512198324224
Encrypt: true
Servertime: 1770587488
Localtime: 2026-Feb-09 05:51:28
Authority: 0
Key: 
Worldid: 1
频道列表:
  [0] World=1, Channel=1, IP=66.66.66.66, Port=10001, Priority=0
=== 测试成功 ===
```

**Java 服务端日志**：
```
===== MessageCodecFactory.getDecoder() 被调用，protobuf.mode=standard =====
===== 返回 StandardProtobufDecoder =====
===== MessageCodecFactory.getEncoder() 被调用，protobuf.mode=standard =====
===== 返回 StandardProtobufEncoder =====
```

### 问题记录

| 问题 | 严重程度 | 状态 | 解决方案 |
| :--- | :--- | :--- | :--- |
| 无 | - | - | - |

### 结论

✅ **测试通过**：Go 客户端与 Java 服务端之间的跨语言通信正常，所有测试项均通过。

## 📚 相关文档

- [01_迁移流程.md](./01_迁移流程.md) - 7 步迁移流程详解
- [02_类型映射.md](./02_类型映射.md) - JProtobuf 到标准 Protobuf 类型映射
- [03_常见问题.md](./03_常见问题.md) - 常见问题与解决方案
- [04_双模式编解码器.md](./04_双模式编解码器.md) - 双模式编解码器实现
